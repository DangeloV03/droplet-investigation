# Slurm settings for json_runner.py --slurm
# Edit partition/account/report_dir for your Della account before submitting.

partition: cpu
cpus_per_task: 1
mem_per_cpu: 4G
# 12 hours. Submitted to Slurm as --time=720 (minutes; avoids HH:MM:SS parsing issues).
time_minutes: 720
job_name: droplet_sim

# Often required on Della — check flex-investigation or run:
#   sacctmgr show assoc user=$USER format=account,partition -p
# account: wjacobs

report_dir: /home/$USER/slurm_reports

setup_cmds:
  - module load anaconda3/2024.10
  - conda activate lattice
  - export LD_LIBRARY_PATH="${CONDA_PREFIX}/lib:${LD_LIBRARY_PATH:-}"

project_root: "/scratch/gpfs/WJACOBS/vd7294/droplet-investigation"
